# DOUPAD 3.2.0 — GitHub Actions build

Push to `main` and `.github/workflows/build-apk.yml` will run source verification, unit tests, assemble the debug APK, and upload `DOUPAD-3.2.0-connectivity-debug` as an Actions artifact.
